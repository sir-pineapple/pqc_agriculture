# kybesp32 is a git submodule stub — the actual Kyber implementation lives in
# the sibling components (kem, indcpa, poly, polyvec, ntt, reduce, cbd,
# fips202, symmetric, verify, randombytes, aes256ctr, sha2).
# This file intentionally registers no sources.
idf_component_register()